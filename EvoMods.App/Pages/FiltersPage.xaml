<Page
    x:Class="EvoMods.App.Pages.FiltersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EvoMods.App.Pages"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Page.Resources>
        <DataTemplate x:Key="FilterRowTemplate" x:DataType="local:FilterRowView">
            <Grid Padding="0,7" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="1">
                    <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock Text="{x:Bind Path}" Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <Border Grid.Column="1" VerticalAlignment="Center"
                        CornerRadius="4" Padding="8,2"
                        Background="{x:Bind ChipBackground}">
                    <TextBlock Text="{x:Bind Chip}" Foreground="{x:Bind ChipForeground}"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                </Border>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="28" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Post-processing filters" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Name="SourceText" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
        </StackPanel>

        <InfoBar x:Name="Notice" Grid.Row="1" IsOpen="False" IsClosable="False" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="0,0,12,0">
            <StackPanel Spacing="18">
                <StackPanel x:Name="OursSection" Spacing="2">
                    <TextBlock Text="EvoMods filters" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <ItemsControl x:Name="OursList" ItemTemplate="{StaticResource FilterRowTemplate}" />
                </StackPanel>

                <StackPanel x:Name="StockSection" Spacing="2">
                    <TextBlock Text="Shipped with the game" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <ItemsControl x:Name="StockList" ItemTemplate="{StaticResource FilterRowTemplate}" />
                </StackPanel>

                <StackPanel x:Name="OtherSection" Spacing="2" Visibility="Collapsed">
                    <TextBlock Text="Installed by something else"
                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <ItemsControl x:Name="OtherList" ItemTemplate="{StaticResource FilterRowTemplate}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
            <Button x:Name="UnlockButton" Click="OnUnlock" />
            <Button x:Name="InstallButton" Click="OnInstall" Style="{StaticResource AccentButtonStyle}" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" TextWrapping="Wrap"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>
    </Grid>
</Page>
