<Page
    x:Class="EvoMods.App.Pages.GamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid Padding="28" RowSpacing="16" AllowDrop="True" DragOver="OnDragOver" Drop="OnDrop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Game content" Style="{StaticResource TitleTextBlockStyle}"
                       FontFamily="{StaticResource BrandDisplayFont}" FontSize="34"
                       CharacterSpacing="20" />
            <TextBlock x:Name="PathText" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       IsTextSelectionEnabled="True" TextWrapping="Wrap" />
        </StackPanel>

        <InfoBar x:Name="Notice" Grid.Row="1" IsOpen="False" IsClosable="False" />

        <Border Grid.Row="2" CornerRadius="8" Padding="16" BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
            <Grid ColumnSpacing="16" RowSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Content"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="ArchiveText" TextWrapping="Wrap" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Disk"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="DiskText" TextWrapping="Wrap" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
            <Button x:Name="UnpackButton" Content="Unpack game content" Click="OnUnpack"
                    Style="{StaticResource AccentButtonStyle}" />
            <Button x:Name="RevertButton" Content="Revert to packed archive" Click="OnRevert" />
            <Button x:Name="PackageButton" Content="Unpack a .kspkg…" Click="OnUnpackPackage" />
        </StackPanel>

        <!-- Only while something is running. A determinate bar and a Cancel that works are the whole
             point of this screen: the archive is ~68 GB and this takes a long time. -->
        <StackPanel Grid.Row="4" x:Name="ProgressPanel" Spacing="6" Visibility="Collapsed">
            <ProgressBar x:Name="Bar" Minimum="0" Maximum="100" Height="6" />
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock x:Name="ProgressText" VerticalAlignment="Center"
                           Style="{StaticResource CaptionTextBlockStyle}" />
                <Button x:Name="CancelButton" Content="Cancel" Click="OnCancel" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="5" CornerRadius="8" Padding="12" BorderThickness="1" MinHeight="80"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
            <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogText" TextWrapping="Wrap" IsTextSelectionEnabled="True"
                           FontFamily="Consolas" FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Text="Drop a .kspkg here, or use the buttons above." />
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
